@model IEnumerable<E_lections.Models.Stranka>

@{
    Layout = "_LayoutAdmin";
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Naziv)
                </td>
                <td>
                    <a asp-action="AzurirajStranku" asp-route-id="@item.ID">Ažuriraj</a> |
                    <a asp-action="DetaljiStranke" asp-route-id="@item.ID">Detalji</a> |
                    <a asp-action="ObrisiStranku" asp-route-id="@item.ID">Obriši</a>
                </td>
            </tr>
        }
    </tbody>
</table>
